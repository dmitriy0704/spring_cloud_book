spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://localhost:8071

eureka:
  client:
    enabled: true

management:
  endpoint.health.show-details: "ALWAYS"
  endpoints.web.exposure.include: "*"